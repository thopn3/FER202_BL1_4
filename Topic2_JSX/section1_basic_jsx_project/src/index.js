import React from 'react';
import ReactDOM from 'react-dom/client';
import RootComp from './RootComponent';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <RootComp/>
  </React.StrictMode>
);